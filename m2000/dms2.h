/*
 * Function to compute GF(2) sign of a matrix over Z/8Z
 * where the matrix is 2n x 2n
 * Returns 0 for +, 255 for -
 *
 */

#ifndef included__dms2
#define included__dms2

extern int dms2(const char *m, const char *name);

#endif
