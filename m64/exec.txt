$(TARGET_TYPE)_OBJECTS+= $(EXECS:%=$($(TARGET_TYPE)OBJDIR)/%.$(OBJEXT))
$(TARGET_TYPE)_EXECS:= $(EXECS:%=$($(TARGET_TYPE)BINDIR)/%)
$(TARGET_TYPE)_TARGETS+= $($(TARGET_TYPE)_EXECS)

COMMAND := $($(TARGET_TYPE)BINDIR)/%: $($(TARGET_TYPE)OBJDIR)/%.$(OBJEXT) $($(TARGET_TYPE)_LIBTARGET) ; @echo Link $$@; rm -f $$@ || true; mkdir -p $($(TARGET_TYPE)BINDIR) || true ; $(LINK)$$@ $($(TARGET_TYPE)_LINK_FLAGS) $$< $($(TARGET_TYPE)_LIBTARGET) $($(TARGET_TYPE)_LIBS)

$(COMMAND)

.PRECIOUS:	$($(TARGET_TYPE)_OBJECTS)
